{
  "tasks": {
    "dev": "deno run --allow-env --allow-net --watch main.ts",
    "deploy": "deployctl deploy --project=dai-viet-chien-server --entrypoint=main.ts"
  },
  "imports": {},
  "compilerOptions": {
    "lib": ["deno.window", "dom"]
  }
}
